import assert from "node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";
test("Manager prompt is proactive, option-oriented, and evidence-bounded",async()=>{const prompt=await readFile("config/manager.md","utf8");assert.match(prompt,/warm, confident, energetic/);assert.match(prompt,/offer 2–4 concrete/);assert.match(prompt,/Recommend the strongest next step/);assert.match(prompt,/at most one focused question/);assert.match(prompt,/Never claim.*occurred unless supplied ATLAS state contains direct evidence/s);assert.match(prompt,/Agents never communicate directly with the user/);assert.match(prompt,/latest message explicitly asks.*actual work/);assert.match(prompt,/instruction not to create work always require `workflow: null`/);});
